define("zText", ["versificationMgr", "dataMgr", "async"], function (versificationMgr, dataMgr, async) {
    var zText = {},
        index = {},
        start = 0,
        bookPositions = [];

    return zText;
});